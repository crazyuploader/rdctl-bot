# PostgreSQL Configuration (for docker-compose)
POSTGRES_USER=rdctl_bot
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=rd_bot

# Database Host (use 'postgres' when running via docker-compose, or 'localhost' for local dev)
# DATABASE_HOST=postgres
